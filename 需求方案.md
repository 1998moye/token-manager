# Token 管理器 插件 — 需求方案

> 日期：2026-05-12
> 状态：待实施
> 版本：v2（重构版）

---

## 1. 项目概述

**项目名称：** Token 管理器
**类型：** Edge 浏览器插件（Manifest V3）
**一句话描述：** 管理 AI API Token 配置，支持一键复制和多平台多模型。
**目标用户：** 使用 CLI 工具（Claude Code、Codex 等）并通过 ccswitch 管理 token 的开发者。

---

## 2. 核心功能（v2 重构）

### 2.1 Token 管理（重构）

| 功能 | 描述 |
|------|------|
| 添加 Token | 录入平台、模型、名称、API Key、API 地址（base URL）、备注 |
| 编辑 Token | 修改已有 Token 信息 |
| 删除 Token | 删除 Token 记录（需确认） |
| 列表展示 | 卡片形式展示所有已录入 Token |
| 搜索过滤 | 按平台、模型或名称搜索 |

### 2.2 一键复制

- **复制全量配置（ccswitch profile JSON 片段）**：点击卡片右上角图标，输出形如下面的 JSON 片段，可直接粘贴进 ccswitch 配置文件的 profiles 父对象：
  ```json
  "<token.name>": {
    "env": {
      "ANTHROPIC_AUTH_TOKEN": "<apiKey>",
      "ANTHROPIC_BASE_URL": "<baseUrl>",
      "ANTHROPIC_DEFAULT_HAIKU_MODEL": "<model>",
      "ANTHROPIC_DEFAULT_OPUS_MODEL": "<model>",
      "ANTHROPIC_DEFAULT_SONNET_MODEL": "<model>",
      "ANTHROPIC_MODEL": "<model>",
      "ANTHROPIC_REASONING_MODEL": "<model>"
    },
    "includeCoAuthoredBy": false
  }
  ```
  说明：5 个 `_MODEL` 字段均使用卡片上的 `model` 值；profile key 使用 `token.name`；末尾 `includeCoAuthoredBy` 固定为 `false`。
- **独立复制 API 地址**：点击 API 地址行框右侧图标，复制 `baseUrl` 单字段。
- **独立复制 API Key**：点击 API Key 行框右侧图标，复制 `apiKey` 单字段。
- API Key 默认脱敏显示（`sk-***xx`），点眼睛图标切换显示/隐藏；复制结果不受显示状态影响（始终复制明文）。
- API 地址明文显示，过长时 ellipsis 截断，悬停 title 展示完整 URL。

### 2.3 数据备份

- **导出：** 将所有 Token 记录导出为 JSON 文件
- **导入：** 从 JSON 文件恢复 Token 记录

### 2.6 防丢体验（新增）

- **表单自动保存** — 正在填写的内容实时自动保存到 localStorage，关闭弹窗/切换页面后重新打开自动恢复，不用担心内容丢失
- **设置页可添加/编辑** — 设置页也支持添加和编辑 Token，作为不间断操作的备选入口

---

## 3. 支持平台及模型

### 3.1 国际大厂

| 平台 | API 地址（默认） | 代表模型 |
|------|-----------------|---------|
| **OpenAI** | `https://api.openai.com/v1` | gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo, o1, o1-mini, o3, o3-mini |
| **Anthropic** | `https://api.anthropic.com` | claude-3-5-sonnet-20241022, claude-3-5-haiku-20241022, claude-3-opus-20240229, claude-3-sonnet-20240229 |
| **Google** | `https://generativelanguage.googleapis.com/v1beta` | gemini-2.0-flash, gemini-2.5-flash, gemini-2.5-pro, gemini-1.5-flash, gemini-1.5-pro |
| **Groq** | `https://api.groq.com/openai/v1` | llama-3.3-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768, gemma-7b-it |
| **Mistral AI** | `https://api.mistral.ai/v1` | mistral-large-latest, mistral-small-latest, codestral-latest |
| **Cohere** | `https://api.cohere.ai/v1` | command-r-plus-08-2024, command-r7b-12-2024, command-medium-nightly |
| **Perplexity** | `https://api.perplexity.ai` | sonar-pro, sonar-reasoning-pro, sonar |

### 3.2 国内大厂

| 平台 | API 地址（默认） | 代表模型 |
|------|-----------------|---------|
| **DeepSeek** | `https://api.deepseek.com` | deepseek-chat, deepseek-coder, deepseek-reasoner (R1) |
| **Kimi (Moonshot)** | `https://api.moonshot.cn/v1` | moonshot-v1-8k, moonshot-v1-32k, moonshot-v1-128k |
| **MiniMax** | `https://api.minimax.chat/v1` | MiniMax-Text-01, MiniMax-Embed-02 |
| **豆包 (字节)** | `https://ark.cn-beijing.volces.com/api/v3` | doubao-pro-32k, doubao-lite-32k, doubao-pro-4k |
| **通义千问 (阿里)** | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus, qwen-plus-latest, qwen-turbo, qwen-max, qwen2.5-72b-instruct |
| **文心一言 (百度)** | `https://qianfan.baidubce.com/v2` | ernie-4.0-8k, ernie-3.5-8k, ernie-speed-128k |
| **混元 (腾讯)** | `https://api.hunyuan.cloud.tencent.com` | hunyuan-turb, hunyuan-lite |
| **智谱 GLM** | `https://open.bigmodel.cn/api/paas/v4` | glm-4-plus, glm-4-flash, glm-4v-plus, glm-z1c-32k |
| **阶跃星辰** | `https://api.stepfun.com/v1` | step-2-16k, step-1-vision-32k |
| **零一万物** | `https://api.lingyiwanwu.com/v1` | yi-lightning, yi-34b-chat |
| **百川智能** | `https://api.baichuan-ai.com/v1` | baichuan4-turbo, baichuan3-turbo |

### 3.3 聚合平台

| 平台 | API 地址（默认） | 代表模型 |
|------|-----------------|---------|
| **SiliconFlow** | `https://api.siliconflow.cn/v1` | 全模型聚合，支持 100+ 模型 |
| **Together AI** | `https://api.together.xyz/v1` | Llama/开源模型加速 |
| **SiliconCloud** | `https://api.siliconcloud.cn/v1` | 加速版模型 |

### 3.4 其他

| 平台 | API 地址（默认） | 代表模型 |
|------|-----------------|---------|
| **xAI Grok** | `https://api.x.ai/v1` | grok-2, grok-2-mini, grok-beta |
| **Ollama (本地)** | `http://localhost:11434/v1` | 本地部署的开源模型 |

---

## 4. 页面结构

### 4.1 入口

插件栏一个图标按钮，点击打开 popup 主界面。

### 4.2 页面构成

| 页面 | 说明 |
|------|------|
| Token 列表页（默认） | 卡片展示所有 Token，含平台图标、名称、模型、API 地址、复制按钮 |
| 添加/编辑页面 | 表单录入或修改 Token 信息 |
| 设置页面 | 全局设置、数据显示偏好、数据备份等 |

---

## 5. 数据结构

每条 Token 记录：

```json
{
  "id": "uuid",
  "platform": "openai",
  "name": "我的 OpenAI Key",
  "apiKey": "sk-xxxxxxxx",
  "baseUrl": "https://api.openai.com/v1",
  "model": "gpt-4o",
  "remark": "备注信息",
  "createdAt": "2026-05-12T00:00:00Z",
  "updatedAt": "2026-05-12T00:00:00Z"
}
```

全局设置：

```json
{
  "sortBy": "updatedAt",
  "sortOrder": "desc",
  "defaultPlatform": "openai"
}
```

---

## 6. 技术方案

### 6.1 技术栈

| 层级 | 技术 |
|------|------|
| 扩展基础 | Manifest V3 |
| 前端框架 | Vue 3 |
| UI 组件库 | Element Plus |
| 后端逻辑 | Service Worker |
| 本地存储 | chrome.storage.local |
| 数据备份 | JSON 文件（导入/导出） |

### 6.2 模块划分

- **TokenStore** — Token 数据的增删改查，基于 chrome.storage
- **PlatformConfig** — 所有平台配置（名称、图标、颜色、默认 base URL、模型列表）
- **ExportService** — JSON 导入/导出
- **ClipboardService** — 复制到剪贴板（平台 | 模型 | Key | URL 格式）
- **PlatformIcons** — 各平台图标和标识

---

## 7. 非功能需求

- 纯浏览器插件，无后端服务
- 数据存储在本地，仅用户可访问
- 所有中文界面
- UTF-8 编码
- 插件包体积尽量精简

---

## 8. 文件结构

```
keyManager/
├── manifest.json              # 插件配置
├── package.json               # 项目依赖
├── vite.config.js             # Vite 构建配置
├── index.html                 # popup 主界面
├── options.html               # 设置页
├── src/
│   ├── main.js               # Vue 入口
│   ├── options.js            # 设置页 Vue 入口
│   ├── App.vue               # popup 根组件
│   ├── OptionsApp.vue         # 设置页根组件
│   ├── components/
│   │   ├── TokenCard.vue     # Token 卡片
│   │   ├── TokenForm.vue     # 添加/编辑表单
│   │   └── ConfirmDialog.vue # 确认删除弹窗
│   ├── services/
│   │   ├── storage.js        # chrome.storage 封装
│   │   └── clipboard.js      # 剪贴板复制
│   ├── data/
│   │   └── platforms.js      # 所有平台配置（名称/baseURL/模型列表）
│   └── styles/
│       └── common.css        # 公共样式
└── public/
    ├── manifest.json         # 插件 manifest
    └── icons/                # 插件图标
```

---

## 2.4 通用 Token 管理（v2.2.0 新增）

### 功能描述
支持保存只需要一个 token 字符串的普通凭证（如 GitHub Token、npm Token、云服务等）。

### 数据字段
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 显示名称，如 "GitHub Token" |
| value | string | 是 | token 字符串本身 |
| category | string | 是 | 分类：git / npm / cloud / other / 自定义 |
| remark | string | 否 | 备注 |

### 预置分类
| 标识 | 显示名 | 颜色 |
|------|--------|------|
| git | Git | #f14e32 |
| npm | NPM | #cb3837 |
| cloud | 云服务 | #3b82f6 |
| other | 其他 | #94a3b8 |

### UI 组织
- Popup 和设置页均通过「AI Token」「通用 Token」标签页切换
- 切换时搜索关键词清空，列表/添加/导入导出行为自动隔离
- 通用 Token 卡片：分类彩色标签 + 名称 + 脱敏 Token 值 + 复制/编辑/删除
- 通用 Token 表单：名称 / Token 值（文本域，支持多行）/ 分类（下拉+自定义输入）/ 备注

### 存储方案
- 独立 storage key：`generalTokens`，与 AI Token（`tokens`）完全隔离
- 导入导出各自独立，数据结构保持纯数组不变

---

## 11. 验证方式

- 表单内容实时自动保存到 localStorage，关闭/切屏后重新打开自动恢复
- 设置页也支持添加和编辑 Token，作为不间断操作的备选入口

1. 在 Edge 中加载插件，验证能正常打开 popup 界面
2. 添加一条 Token（选择平台、输入 Key、确认 API 地址），验证保存成功
3. 验证平台下拉包含所有 18 个平台
4. 验证选择平台后，模型下拉自动填充该平台的模型列表
5. 验证选择平台后，API 地址自动填充默认值
6. 点击眼睛图标，验证 key 显示/隐藏切换
7. 点击复制，验证剪贴板内容为「平台名 | 模型名 | API Key | API 地址」格式
8. 编辑 Token，验证修改保存正确
9. 删除 Token，验证记录移除
10. 搜索过滤，验证按平台/模型/名称筛选正确
11. 导出 JSON，验证文件内容完整
12. 导入 JSON，验证数据恢复正确
13. 设置页面，验证全局配置保存正确

### 通用 Token 验证（v2.2.0 新增）

14. 在 popup 中切换到「通用 Token」标签，验证列表为空时显示「还没有通用 Token」
15. 添加一条通用 Token（填写名称、Token 值、选择分类），验证保存成功
16. 验证通用 Token 卡片显示分类彩色标签、名称、脱敏 Token 值
17. 点击眼睛图标，验证 Token 值显示/隐藏切换
18. 点击复制，验证剪贴板内容为 Token 明文
19. 编辑通用 Token，验证修改保存正确
20. 删除通用 Token，验证记录移除
21. 搜索过滤，验证按名称/分类/备注筛选正确
22. 导出通用 Token，验证为纯数组格式
23. 导入通用 Token，验证数据恢复正确
24. 验证设置页也支持通用 Token 的添加/编辑/删除
25. 切换回「AI Token」标签，验证 AI Token 功能不受影响