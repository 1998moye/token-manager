# AI Token Manager 插件 — 实现计划 v2

> **面向 AI 代理的工作者：** 逐任务实现此计划。

**目标：** 构建一个 Edge 浏览器插件（MV3），管理 AI API Token，支持一键复制和多平台多模型。简洁现代风格。

**架构：** Vite + Vue 3 + Element Plus 构建插件前端，chrome.storage.local 存储数据。popup 为单页应用，设置页支持完整 Token 管理。

**技术栈：** Manifest V3 / Vue 3 / Element Plus / Vite / chrome.storage API

---

## 文件结构

```
keyManager/
├── manifest.json                  # 插件配置（MV3）
├── package.json                   # 项目依赖
├── vite.config.js                 # Vite 构建配置
├── index.html                     # popup 入口 HTML
├── options.html                   # options 页面入口 HTML
├── src/
│   ├── main.js                   # popup Vue 入口
│   ├── options.js                # options Vue 入口
│   ├── App.vue                   # popup 根组件
│   ├── OptionsApp.vue            # 设置页根组件（支持添加/编辑 Token）
│   ├── components/
│   │   ├── TokenCard.vue         # Token 卡片组件
│   │   ├── TokenForm.vue         # 添加/编辑表单组件
│   │   └── ConfirmDialog.vue     # 确认删除弹窗
│   ├── services/
│   │   ├── storage.js            # chrome.storage 封装
│   │   └── clipboard.js          # 剪贴板复制
│   ├── data/
│   │   └── platforms.js          # 所有平台配置（18个平台 + 模型列表）
│   ├── composables/
│   │   └── useAutoSave.js        # 表单自动保存 composable
│   └── styles/
│       └── common.css            # 公共样式
└── public/
    ├── manifest.json             # 插件 manifest
    └── icons/                    # 插件图标
```

---

## 任务列表

### 任务 1：初始化项目重构 — 清理旧文件，保留基础配置

**文件：**
- 删除：旧 src/ 目录（除保留文件外）
- 创建：`package.json`（更新依赖）
- 创建：`vite.config.js`（更新配置）
- 创建：`public/manifest.json`（MV3）
- 创建：`index.html`
- 创建：`options.html`

- [ ] **步骤 1：删除旧文件，保留基础目录**

保留：`public/icons/` 目录（已有的图标）、`package.json`、`vite.config.js`、`public/manifest.json`

更新 `package.json` 依赖：
```json
{
  "name": "ai-token-manager",
  "version": "2.0.0",
  "type": "module",
  "description": "AI API Token 管理插件，支持一键复制和多平台多模型",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "element-plus": "^2.8.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-static-copy": "^2.0.0"
  }
}
```

- [ ] **步骤 2：更新 vite.config.js**

```javascript
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { viteStaticCopy } from 'vite-plugin-static-copy';
import { resolve } from 'path';

export default defineConfig({
  plugins: [
    vue(),
    viteStaticCopy({
      targets: [
        {
          src: 'manifest.json',
          dest: ''
        }
      ]
    })
  ],
  build: {
    outDir: 'dist',
    emptyOutDir: true,
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html'),
        options: resolve(__dirname, 'options.html')
      }
    }
  }
});
```

- [ ] **步骤 3：更新 manifest.json**

```json
{
  "manifest_version": 3,
  "name": "AI Token Manager",
  "version": "2.0.0",
  "description": "管理 AI API Token，支持一键复制和多平台多模型",
  "permissions": ["storage"],
  "action": {
    "default_popup": "index.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "32": "icons/icon32.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "options_page": "options.html"
}
```

- [ ] **步骤 4：创建 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Token Manager</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.js"></script>
</body>
</html>
```

- [ ] **步骤 5：创建 options.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Token Manager 设置</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/options.js"></script>
</body>
</html>
```

---

### 任务 2：平台配置数据 — 18个平台 + 所有模型

**文件：**
- 创建：`src/data/platforms.js`

包含 18 个平台的完整配置：
- openai / anthropic / google / groq / mistral / cohere / perplexity
- deepseek / moonshot / minimax / doubao / dashscope / wenxin / hunyuan / zhipu / stepfun / lingyi / baichuan
- siliconflow / together / siliconcloud / xai / ollama

每个平台包含：id / name / color（主题色） / logo（单字母） / baseUrl（默认API地址） / models（模型列表）

具体配置内容见需求方案.md 章节3。

---

### 任务 3：服务层 — storage / clipboard / autoSave

**文件：**
- 创建：`src/services/storage.js`
- 创建：`src/services/clipboard.js`
- 创建：`src/composables/useAutoSave.js`
- 创建：`src/styles/common.css`

**src/services/storage.js：** chrome.storage 封装，getTokens / saveTokens / addToken / updateToken / deleteToken / getSettings / saveSettings

**src/services/clipboard.js：** copyToClipboard，返回 { success, error }

**src/composables/useAutoSave.js：** 表单自动保存 composable
```javascript
// 监听表单字段变化，自动保存到 localStorage
// key: 'token-form-draft'
// 恢复：打开弹窗时检查 draft，有则自动填充
// 清除：提交成功后 / 关闭弹窗时清除 draft
```

**src/styles/common.css：** 全局样式（简洁现代风格）
- 背景色：#f8fafc（极浅灰蓝）
- 卡片色：#ffffff
- 主题色：#3b82f6（蓝色）
- 圆角：8px
- 阴影：轻柔阴影

---

### 任务 4：TokenCard 组件（重新设计）

**文件：**
- 创建：`src/components/TokenCard.vue`

简洁现代风格：
- 卡片左侧彩色条（平台主题色）
- 平台 logo 圆形图标
- 名称 + 模型名（大字）
- API 地址（淡化，小字，可复制）
- API Key（monospace 字体，隐藏显示切换）
- 复制按钮（一键复制整个配置）
- 右上角：编辑 / 删除

---

### 任务 5：TokenForm 组件（重新设计）

**文件：**
- 创建：`src/components/TokenForm.vue`

表单字段：
1. 平台选择（el-select 下拉，带颜色圆点）
2. 模型选择（el-select，下拉选 + 可手动输入）
3. 名称（el-input）
4. API 地址（el-input，带默认值自动填充）
5. API Key（el-input，monospace 字体）
6. 备注（el-input，可选）

特性：
- 选择平台后自动填充默认 API 地址和模型列表
- 使用 useAutoSave composable 实现表单自动保存
- 表单验证：平台/名称/API Key 必填

---

### 任务 6：ConfirmDialog 组件

**文件：**
- 创建：`src/components/ConfirmDialog.vue`

简单的确认删除弹窗，el-dialog 封装。

---

### 任务 7：App.vue 主界面（重新设计）

**文件：**
- 创建：`src/main.js`
- 创建：`src/App.vue`

简洁现代风格主界面：
- 顶部：标题 + 搜索框 + 添加按钮
- Token 列表：卡片网格
- 底部：导入 / 导出
- 无空状态时显示引导文案

---

### 任务 8：OptionsApp.vue 设置页（包含 Token 管理）

**文件：**
- 创建：`src/options.js`
- 创建：`src/OptionsApp.vue`

设置页包含：
- Token 列表（与 popup 相同的卡片）
- 添加/编辑/删除 Token
- 搜索过滤
- 导入/导出
- 全局设置（排序方式等）

---

### 任务 9：构建和测试

- 运行 npm install && npm run build
- 验证 dist/ 目录
- Edge 加载插件（用户手动验证）

---

## 自检清单

- [ ] 18 个平台全部包含在 platforms.js
- [ ] 选择平台后模型下拉自动更新
- [ ] 选择平台后 API 地址自动填充
- [ ] 表单自动保存恢复功能正常
- [ ] 一键复制格式为「平台名 | 模型名 | Key | URL」
- [ ] 样式简洁现代，无多余装饰