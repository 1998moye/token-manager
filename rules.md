# 工作流规则 — 强制约束

> ⚠️ **系统指令，不是建议。违反任何一条 = 强制回退。没有例外。**

---

## 第一节：触发条件 → 强制动作

| 编号 | 触发条件 | 强制动作 |
|------|---------|---------|
| 1 | 用户提新需求 / 修改功能 | 先更新 `需求.md` → 再写方案到 `方案.md` → **等用户说"确认实现"后** → 再写代码 |
| 2 | 用户报 Bug / 发现问题 | 先按"更改记录分片规则"定位当前活动 `更改记录.md` → 写清问题 + 根因 → 再修复 → 修复后再补解决方案 + 文件 |
| 3 | 任何代码变更结束 | 按"更改记录分片规则"追加到当前活动 `更改记录.md`，写明改了哪些文件及路径 |
| 4 | 需要上下文 / 开始工作 | 读 `tech.md`（如存在），遵守其中的架构和样式 |
| 5 | 写新方案 | 写入 `方案.md`，包含：问题、根因、候选方案、推荐方案及理由、修改文件、验证方式 |

---

## 第二节：不可打破的规则

1. **编码前必须确认方案**
   - 用户未说"确认实现""可以做了""开始吧"等明确同意前，禁止写代码
   - 禁止"先写一点看看"

2. **方案不许只留在对话里**
   - 每个方案必须写入 `方案.md`
   - 禁止方案只留在聊天消息中

3. **变更不许漏记**
   - 每次代码变更结束后必须按"更改记录分片规则"更新当前活动 `更改记录.md`
   - 格式：先写"什么问题 / 为什么改"，再写"怎么改的"

4. **Bug 历史不许跳过**
   - 每次改代码前必须读 `问题.md`
   - 若同类 Bug 之前修过，复用已验证的方案

5. **设计约束不许违反**
   - 本章下方"项目特定约束"中的规则，与上面通用规则具有同等效力
   - 修改代码前必须先读并遵守

---

## 第三节：方案模板（写入 `方案.md` 时必须包含）

```markdown
## vX.Y.Z（待定）标题

### 问题描述
- 症状：...

### 根因分析
- ...

### 候选方案

#### 方案 A
- 描述：...
- 优点：...
- 缺点：...

#### 方案 B
- ...

### 推荐方案
- 采用方案 X，理由：...

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `path/to/file` | 描述 |

### 验证方式
1. ...
```

---

## 第四节：变更记录格式

### 4.1 更改记录分片规则（强制）

- 根目录 `更改记录.md` 永远是**当前活动更改记录文件**，所有新增记录只写入当前活动文件
- 每次准备写入更改记录前，必须先检查是否需要分片：
  1. **跨日分片**：如果当前日期与 `更改记录.md` 顶部最新记录日期不同，必须先分片
  2. **长度分片**：如果 `更改记录.md` 超过 800 行或 50,000 字符，必须先分片
- 分片流程：
  1. 将现有 `更改记录.md` 重命名为历史分片，命名格式：`更改记录-YYYY-MM-DD.md`
  2. 如果同名文件已存在，追加序号：`更改记录-YYYY-MM-DD-2.md`、`更改记录-YYYY-MM-DD-3.md`
  3. 新建空的 `更改记录.md`，写入标题 `# 更改记录` 后，再写本次新记录
- 禁止把新的变更继续追加到已经归档的历史分片
- 禁止因为分片而丢失、覆盖、截断旧记录
- 如果只是阅读历史记录，优先读当前 `更改记录.md`；需要追溯历史时，再按日期读取历史分片

### 4.2 单条记录格式

```markdown
## vX.Y.Z (YYYY-MM-DD) 标题

### Bug 修复
- **[修复名]** 描述
  - 根因：...
  - 解决方案：...
  - 文件：`路径/文件`

### 新增特性
- **[特性名]** 描述
  - 文件：`路径/文件`
```

---

## 第五节：收尾检查清单（强制）

任何任务结束前必须逐项勾选：

- [ ] 已按"更改记录分片规则"更新当前活动 `更改记录.md`？
- [ ] 用户报的 Bug 已记录？
- [ ] `需求.md` 已同步（如有范围变更）？
- [ ] `tech.md` 已同步（如有架构变更）？
- [ ] 已验证没有回归问题？

---

## 第六节：绝对禁止（出现即回退）

- 未读 `CLAUDE.md` 和 `rules.md` 就写代码
- 未读 `问题.md` 就改代码
- 未写方案或未将方案写入 `方案.md` 就动手实现
- 方案只留在对话里、不写入文件
- 代码变更后不按"更改记录分片规则"更新当前活动 `更改记录.md`
- 以"对话太长了""规则记不清了"为由跳过自检
- 以"先写一点看看"为由边写方案边改代码

---

## 附录：项目特定约束（⚠️ 每个项目自行补充）

> 把本项目特有的设计约束、技术限制、UI 规范写在这里。
> 以下为占位符，复制到新项目后替换为实际约束。

### {约束类别 A}（🏆 优先级）
- ...

### {约束类别 B}（🏆 优先级）
- ...
