# 方案

## v2.0.5（待定）卡片被压缩 → 改为固定高度 + 列表滚动

### 问题描述
- 症状：popup 中添加多个 API（Token）后，每张 TokenCard 的高度被自动压缩，以塞进 popup 视口。用户期望卡片大小不变，超出时在列表区滚动。

### 根因分析
1. `src/styles/common.css` 中 body 仅设置 `width: 400px; min-height: 480px;`，没有 `height`/`max-height`。当卡片增多，body 会被内容撑高，直到达到浏览器 popup 的内置上限（Edge/Chrome ≈ 600px）才被截断。
2. `src/App.vue` 中 `.app { height: 100vh; }`，因此 `.app` 跟随 body 的实际高度变化。
3. `.token-list` 是 `display: flex; flex-direction: column;`，而 `.token-card` 默认 `flex-shrink: 1`。当 `.token-list` 自身高度也被压缩、内容总高超过容器时，flex 容器并不会触发 `overflow-y: auto`，而是按比例压缩每一个子项 → 卡片肉眼可见地变小。

两个原因叠加：①body 没固定高度 → ②flex 子项默认会被压缩。

### 候选方案

#### 方案 A（仅修 flex-shrink）
- 描述：只给 `.token-card` 加 `flex-shrink: 0;`。
- 优点：改动最小。
- 缺点：popup 高度仍随卡片数动态变化（卡片少时矮，卡片多时高），高度跳变体验不佳，且仍依赖浏览器上限被动截断。

#### 方案 B（仅固定 body 高度）
- 描述：把 body 改为 `height: 600px`。
- 优点：popup 高度稳定。
- 缺点：未解决 flex 子项被压缩问题，临界状态下卡片仍可能被略微压缩。

#### 方案 C（推荐：组合 A+B）
- 描述：
  1. `src/styles/common.css` 中 body 由 `min-height: 480px` 改为固定 `height: 600px`（保持 400×600 的稳定 popup 尺寸）。
  2. `src/components/TokenCard.vue` 中 `.token-card` 增加 `flex-shrink: 0;`。
- 优点：popup 尺寸恒定不跳变；卡片始终保持设计高度；超出时 `.token-list` 触发垂直滚动（其 `overflow-y: auto` 已就绪，滚动条样式已在 common.css 中定义）。
- 缺点：无明显缺点。600px 在主流显示器上完全适配。

### 推荐方案
采用方案 C。

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `src/styles/common.css` | body 由 `min-height: 480px` 改为 `height: 600px` |
| `src/components/TokenCard.vue` | `.token-card` 增加 `flex-shrink: 0;` |

### 验证方式
1. 加载插件，打开 popup，无 Token 时显示空状态，popup 高度为 600px，不会塌缩。
2. 添加 1~3 个 Token，每张卡片高度与原设计一致（约 110~130px），列表区无滚动条。
3. 继续添加至 6+ 个 Token，每张卡片高度依然不变，`.token-list` 出现垂直滚动条，header 与 footer 保持固定不动。
4. 滚动列表，header 与 footer 不随之滚动。
5. 搜索过滤后卡片数减少时，列表无滚动，popup 总高仍为 600px。

---

## v2.0.6（待定）卡片同时突出 API 地址与 API Key，两者均可单独复制

### 问题描述
- 症状：用户配置模型时同时需要 API 地址和 API Key 两个字段，但 TokenCard 当前布局：
  - API 地址（baseUrl）：单行灰色小字（`.token-baseurl`，`color: #94a3b8`），无背景框、无复制按钮，视觉次要。
  - API Key：浅色背景行框 + 眼睛切换 + 蓝色「复制」按钮——但这个「复制」按钮复制的不是单纯 Key，而是 `平台名 | 模型 | Key | API地址` 拼接全量配置（为 ccswitch 准备）。
- 期望：API 地址与 API Key **视觉对等、均可独立复制**；同时保留为 ccswitch 准备的全量拼接复制能力。

### 根因分析
1. 当前 UI 把全量拼接复制行为放在了 Key 行框的「复制」按钮上，按钮文案/位置易被误解为「只复制 Key」。
2. API 地址未被纳入"行框 + 复制按钮"的视觉模式，与 Key 视觉权重不对等。
3. 没有独立的「复制 API 地址」按钮，用户只能手选高亮再复制。

### 候选方案

#### 方案 A（推荐：两行框对等 + 全量复制移到头部 icon）
- 描述：
  1. 在 `card-actions`（卡片右上角，与「编辑」「删除」并列）新增一个「复制全量配置」图标按钮，复用现有 `copyTokenConfig(token, platformName)` 逻辑，复制 `平台名 | 模型 | Key | API地址`。
  2. API 地址改造为独立行框：与 Key 行框样式一致（浅色背景、圆角、等宽字体），右侧放一个小复制 icon，明文显示 URL（过长 ellipsis 截断，`title` 属性展示完整 URL）。
  3. API Key 行框保留眼睛切换，原蓝色「复制」按钮改为只复制 Key 本身（去掉文案，与 URL 行的复制 icon 风格统一为图标按钮）。
- 优点：
  - URL 与 Key 视觉对等，两者均可独立一键复制。
  - 全量复制能力不丢失，且 icon 化后不占内容区空间。
  - 按钮含义清晰：行内 icon = 复制本行字段；头部 icon = 复制全量配置。
- 缺点：
  - 卡片高度增加约 32px（多一个行框），需验证 popup 600px 下 4~5 个卡片仍能完整展示首屏。
  - 头部 icon 数量从 2 个变为 3 个，需保证排版不挤压标题区。

#### 方案 B（合并行框：URL 与 Key 共享一个行框，分两行）
- 描述：URL 与 Key 上下两行塞进同一个浅色行框，每行右侧各自一个复制 icon。
- 优点：高度增加最少（约 16px）。
- 缺点：信息密度高，视觉拥挤；URL 与 Key 共用一个背景容器，"独立性"不够明确；眼睛切换的位置在分行情况下不直观。

#### 方案 C（仅给原 URL 行加一个复制 icon，不改 Key 行框）
- 描述：保持 URL 灰色小字，仅在其右侧加一个 icon 按钮；Key 行框不动。
- 优点：改动最小。
- 缺点：URL 视觉仍弱于 Key，未达到"两者突出"的需求；且「复制」按钮仍承担全量复制功能，按钮含义模糊问题没解决。

### 推荐方案
采用方案 A（用户已确认：全量复制保留并移到头部 icon 区；API 地址明文显示）。

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `src/components/TokenCard.vue` | ①`.card-actions` 新增「复制全量配置」图标按钮（icon：双方块/clipboard）；②API 地址改为浅色行框结构 `.token-url-row`，右侧加复制 icon；③Key 行框去掉文字「复制」按钮，改为纯 icon 复制按钮（仅复制 Key 本身）；④引入 `copyToClipboard` 用于复制单字段；⑤toast 提示文案区分（"已复制 API 地址""已复制 Key""已复制配置"）。 |

### 验证方式
1. 卡片显示时，API 地址与 API Key 各自在独立浅色行框中、视觉权重相同。
2. 点击 API 地址行框右侧 icon，剪贴板内容 = 该 Token 的 `baseUrl`，toast 显示「已复制 API 地址」。
3. 点击 API Key 行框右侧 icon，剪贴板内容 = 该 Token 的 `apiKey`（明文，无脱敏），toast 显示「已复制 Key」。
4. 点击眼睛 icon，Key 在明文/脱敏间切换，不影响复制结果。
5. 点击卡片右上角新增的「复制全量配置」icon，剪贴板内容 = `平台名 | 模型 | API Key | API地址`，toast 显示「已复制配置」。
6. URL 过长时行框内 ellipsis 截断，悬停 title 展示完整 URL。
7. 头部 actions 排列：复制全量配置 | 编辑 | 删除，三个 icon 间距均匀，不挤压标题。
8. 添加 5 个 Token，popup 600px 内首屏完整展示约 4 张卡片，第 5 张可滚动到。

---

## v2.0.7（待定）「复制完整配置」格式改为 ccswitch profile JSON 片段

### 问题描述
- 当前「复制完整配置」按钮输出：`MiniMax | MiniMax-M2.7 | sk-cp-... | https://api.minimaxi.com/anthropic`（管道符拼接）
- 实际 ccswitch 配置文件期望的是 JSON profile 块：
  ```json
  {
    "env": {
      "ANTHROPIC_AUTH_TOKEN": "...",
      "ANTHROPIC_BASE_URL": "...",
      "ANTHROPIC_DEFAULT_HAIKU_MODEL": "...",
      "ANTHROPIC_DEFAULT_OPUS_MODEL": "...",
      "ANTHROPIC_DEFAULT_SONNET_MODEL": "...",
      "ANTHROPIC_MODEL": "...",
      "ANTHROPIC_REASONING_MODEL": "..."
    },
    "includeCoAuthoredBy": false
  }
  ```
- 管道符格式无法直接粘贴进 ccswitch 配置文件，用户每次都得手动转换。

### 根因分析
1. 早期需求方案 2.2 节误以为 ccswitch 接受 `平台 | 模型 | Key | URL` 拼接格式，实际上 ccswitch 用的是环境变量 JSON 块（每个 profile 一个 `env` 对象 + 可选 `includeCoAuthoredBy`）。
2. `src/services/clipboard.js` 中 `copyTokenConfig` 直接按错误的拼接格式实现。

### 候选方案

#### 方案 A（推荐：输出带 profile 名的 JSON 片段）
- 描述：`copyTokenConfig` 改为输出如下字符串（4 空格缩进，便于直接粘贴进 ccswitch 配置文件的 profiles 父对象）：
  ```
  "<token.name>": {
    "env": {
      "ANTHROPIC_AUTH_TOKEN": "<apiKey>",
      "ANTHROPIC_BASE_URL": "<baseUrl>",
      "ANTHROPIC_DEFAULT_HAIKU_MODEL": "<model>",
      "ANTHROPIC_DEFAULT_OPUS_MODEL": "<model>",
      "ANTHROPIC_DEFAULT_SONNET_MODEL": "<model>",
      "ANTHROPIC_MODEL": "<model>",
      "ANTHROPIC_REASONING_MODEL": "<model>"
    },
    "includeCoAuthoredBy": false
  }
  ```
- 实现：用 `JSON.stringify(profileObj, null, 2)`，外层包一个 `"name": ...` 的 key（手工拼接 key + JSON.stringify(value, null, 2)，避免再做一次 escape）。
- 优点：可直接粘贴进 ccswitch profiles 对象；所有 5 个 model 字段填同一个 model；带 `includeCoAuthoredBy: false`（用户已确认）；profile name 用 token.name，由用户负责命名。
- 缺点：粘贴位置需要用户自己在父对象 `{ "profiles": { ... } }` 的对应位置插入；如果同名 token 会产生重复 key（由用户自己解决）。

#### 方案 B（输出纯 JSON 对象，不带 profile 名）
- 描述：输出 `{ "env": {...}, "includeCoAuthoredBy": false }`，profile name 完全由用户手动加。
- 优点：是合法独立 JSON。
- 缺点：与用户选择「带 profile 名」相悖，每次粘贴都得手动写一遍 name。

#### 方案 C（双格式选择，设置页加开关）
- 描述：保留两种格式，在设置页加单选「复制格式：管道符 / JSON 片段」。
- 优点：兼容老用户。
- 缺点：当前用户明确要 JSON 片段，且这是一个错误格式被修正，不需要保留旧选项；增加额外 UI 与设置项。

### 推荐方案
采用方案 A（用户已确认：5 个 _MODEL 全部填同一个 model；带 `includeCoAuthoredBy: false`；带 profile 名）。

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `src/services/clipboard.js` | `copyTokenConfig(token, platformName)` 重写为输出 ccswitch profile JSON 片段；`platformName` 参数不再需要（profile name 用 `token.name`），保留参数兼容调用方但忽略 |
| `src/components/TokenCard.vue` | 调用处不变，但 toast 文案保持「已复制配置」即可；可选：tooltip 文案从「复制完整配置（用于 ccswitch）」改为「复制 ccswitch 配置块」 |
| `需求方案.md` | 2.2 节「复制全量配置」的格式描述更新为 JSON 片段示例 |

### 验证方式
1. 点击「复制完整配置」icon，剪贴板内容为 7 字段 env JSON 片段（profile name = token.name），可直接粘贴进 ccswitch 配置文件的 profiles 父对象中。
2. apiKey / baseUrl / model 中如含特殊字符（双引号、反斜杠、换行），输出仍是合法 JSON 值（依赖 `JSON.stringify` 自动 escape）。
3. token.name 含中文/空格时，profile key 形如 `"我的 minimax": {...}`，仍是合法 JSON。
4. 5 个 _MODEL 字段值均等于 `token.model`。
5. 末尾包含 `"includeCoAuthoredBy": false`。
6. Toast 显示「已复制配置」。

---

## v2.0.8（待定）插件改名：AI Token Manager → Token 管理器

### 问题描述
- 当前插件展示名为「AI Token Manager」，用户要求改为中文且不带「AI」。
- 用户选定的新名：「Token 管理器」（保留 Token 技术词，主体中文）。

### 根因分析
- 命名只是历史遗留，未涉及功能。涉及多处显示：浏览器扩展名、popup 顶部标题、设置页标题、设置页脚注、HTML title、npm 包名等。

### 候选方案

#### 方案 A（推荐：替换所有运行时可见 & 项目元数据，跳过历史文档）
- 描述：
  - **运行时可见**（必改）：
    - `public/manifest.json` `name`
    - `index.html` `<title>`
    - `options.html` `<title>`
    - `src/App.vue` 顶部 H1
    - `src/OptionsApp.vue` 顶部 H1 + 脚注版本说明
  - **npm 元数据**（必改，npm 包名不接受中文）：
    - `package.json` `name`：`ai-token-manager` → `token-manager`
    - `package-lock.json` 顶层 `name` 跟随
  - **注释 / log**（建议改，保持口径一致）：
    - `generate-icons.js` 注释与 `console.log`
  - **项目文档**（建议改顶部标题）：
    - `需求方案.md` 标题与「项目名称」字段
  - **历史文档不动**（`更改记录.md`、`实现计划.md`、`实现计划v2.md`、`dist/`）：这些是已发生记录或编译产物，改了反而破坏可追溯性。`dist/` 在下次 `vite build` 时自动更新。
- 优点：覆盖所有用户可见位置，历史可追溯。
- 缺点：无明显缺点。

#### 方案 B（只改扩展名 manifest.json，其他不动）
- 描述：仅改 `public/manifest.json` 的 `name`，其余保持英文。
- 优点：改动最少。
- 缺点：popup 顶部标题、设置页标题仍是「AI Token Manager」，与扩展名不一致。

### 推荐方案
采用方案 A。

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `public/manifest.json` | `name`: `"AI Token Manager"` → `"Token 管理器"` |
| `index.html` | `<title>AI Token Manager</title>` → `<title>Token 管理器</title>` |
| `options.html` | `<title>Token Manager 设置</title>` → `<title>Token 管理器 - 设置</title>` |
| `src/App.vue` | `<h1>Token Manager</h1>` → `<h1>Token 管理器</h1>` |
| `src/OptionsApp.vue` | H1 与脚注的 `AI Token Manager` → `Token 管理器` |
| `package.json` | `name`: `"ai-token-manager"` → `"token-manager"` |
| `package-lock.json` | 顶层 `name` 同步 |
| `generate-icons.js` | 注释与 `console.log` 中的 `AI Token Manager` → `Token 管理器` |
| `需求方案.md` | 1 行标题 + 「项目名称」字段 |

### 验证方式
1. `vite build` 通过，`dist/manifest.json` 中 `name` 为「Token 管理器」。
2. 重新加载扩展，浏览器扩展列表显示「Token 管理器」。
3. 点击插件图标，popup 顶部显示「Token 管理器」。
4. 打开设置页，浏览器 tab 标题「Token 管理器 - 设置」；页面 H1「Token 管理器」；脚注「Token 管理器 v2.0.0」。
5. 项目内 grep 不再有运行时可见的 `AI Token Manager` 字样（历史文档与 dist 外）。

---

## v2.1.0（待定）DataLearner 模型数据替换 + 在线更新功能

### 问题描述
- 症状：当前 `platforms.js` 中的模型列表老旧，很多模型名已过时或缺新模型（如 o3、o4-mini、Gemini-3、GLM-5 等均未包含）
- 用户要求：用 DataLearner 最新模型数据替换，并在插件内增加手动更新模型列表的功能

### 根因分析
- 模型迭代极快，硬编码在代码中无法实时同步
- 插件发版周期长，模型列表滞后严重

### 候选方案

#### 方案 A
- 描述：仅替换 `platforms.js` 中的硬编码模型列表
- 优点：简单直接
- 缺点：下次模型更新又要改代码

#### 方案 B（推荐）
- 描述：替换硬编码模型 + 增加「一键更新模型列表」功能
  - 按钮放在设置页，点击后向 datalearner.com 发起请求
  - 解析 HTML 提取模型名，按关键词匹配到各平台
  - 更新后存储到 `chrome.storage`，优先级高于硬编码
  - 提供「恢复默认」按钮
- 优点：用户可自主更新，一劳永逸
- 缺点：需要处理 CORS 和 HTML 解析

### 推荐方案
- 采用方案 B

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `src/data/platforms.js` | 用 DataLearner 最新数据替换各平台模型列表 |
| `src/services/modelUpdater.js` | 新增：在线模型更新逻辑（fetch + 解析 + 映射） |
| `src/services/storage.js` | 新增读写自定义模型列表的方法 |
| `src/OptionsApp.vue` | 增加「更新模型列表」「恢复默认」按钮 |
| `src/data/platforms.js` | `getModelsByPlatform` 改为优先返回 storage 中自定义列表 |

### 验证方式
1. `platforms.js` 硬编码列表已更新为 DataLearner 最新模型
2. 设置页有「更新模型列表」按钮，点击后各平台模型刷新
3. 更新后的模型列表持久化到 storage
4. 重启插件后仍使用最新模型列表
5. 「恢复默认」可回到硬编码版本

---

## v2.1.1（待定）表单增加「API 模型」字段，与「官方模型」区分

### 问题描述
- 用户购买第三方 API（如中转商、聚合平台）时，官方模型名和实际 API 调用时用的模型名可能不同
- 例如：官方模型选 `gpt-4o`，但第三方 API 要求调用时写 `gpt-4o-2024-08-06` 或 `openai/gpt-4o`
- 当前表单只有一个「模型」字段，无法同时记录两者；复制 ccswitch 配置时也只能用这一个名字

### 根因分析
- 早期设计只考虑了「官方模型名 = API 模型名」的场景，没有覆盖第三方 API 中转场景

### 候选方案

#### 方案 A（推荐：双字段分离）
- 描述：
  1. 表单保留「模型」字段（从平台下拉列表选择官方模型名，用于展示）
  2. 新增「API 模型」字段（自由文本输入，用于实际 API 调用）
  3. 复制/导出 ccswitch 配置时，`_MODEL` 字段统一使用 `apiModel`（若未填则回退到 `model`）
  4. TokenCard 中同时显示两个模型名（若不同），或在模型行标注「API: xxx」
  5. 向后兼容：旧数据没有 `apiModel` 时，行为与之前一致（`model` 兼做 API 模型名）
- 优点：清晰区分展示名和调用名，覆盖第三方 API 场景；向后兼容
- 缺点：表单多一个字段

#### 方案 B（单字段，加提示让用户手动覆盖）
- 描述：模型选择框仍只有一个，但允许用户自由编辑，文档提示「可修改为 API 实际模型名」
- 优点：改动最小
- 缺点：用户容易混淆，不知道改的是展示名还是调用名；复制和展示混在一起

### 推荐方案
采用方案 A

### 关键修改文件
| 文件 | 变更 |
|------|------|
| `src/components/TokenForm.vue` | ①新增「API 模型」输入框（在「模型」下方）；②编辑模式时回填 `apiModel`；③提交数据包含 `apiModel` |
| `src/components/TokenCard.vue` | ①模型显示区：若 `apiModel` 存在且与 `model` 不同，显示 `model (API: apiModel)`；②复制配置时传 `apiModel` 给 clipboard |
| `src/services/clipboard.js` | `copyTokenConfig` 中 5 个 `_MODEL` 字段改用 `token.apiModel \|\| token.model` |
| `src/composables/useAutoSave.js` | 草稿保存/恢复包含 `apiModel` |

### 验证方式
1. 添加 Token 时，「模型」从下拉选官方模型，「API 模型」可留空或填自定义名
2. 若 API 模型留空，复制配置时 `_MODEL` 用官方模型名（向后兼容）
3. 若 API 模型填写了 `openai/gpt-4o`，复制配置时 `_MODEL` 用这个值
4. TokenCard 中模型显示为 `gpt-4o (API: openai/gpt-4o)`
5. 编辑旧 Token（无 `apiModel`），表单中 API 模型为空，保存后行为不变
