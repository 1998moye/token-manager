# 更改记录

## v2.1.0 (2026-05-13) DataLearner 模型数据替换 + 在线更新功能

### 变更
- **[模型列表更新]** 从 DataLearner (https://www.datalearner.com/ai-models/pretrained-models) 抓取全部 18 页共 823 个模型，按关键词映射到各平台，替换 `platforms.js` 中老旧的硬编码模型列表
  - OpenAI: 68 个模型（新增 gpt-5 系列、o3/o4-mini 等）
  - Anthropic: 20 个模型（新增 claude-opus-4-7、claude-sonnet-4-6 等）
  - Google: 59 个模型（新增 gemini-3 系列、gemma-4 等）
  - DeepSeek: 38 个模型（新增 deepseek-v4 系列等）
  - 通义千问: 110 个模型（新增 qwen3-5、qwen3-6 系列等）
  - 智谱 GLM: 35 个模型（新增 glm-5、glm-5-1 等）
  - 其他平台均有更新
  - 聚合平台（groq、siliconflow、siliconcloud、together、ollama）保留原有 API ID 不变
  - 文件：`src/data/platforms.js`
- **[在线模型更新]** 新增 `src/services/modelUpdater.js`，支持从 DataLearner 实时抓取模型数据并映射到平台
  - `fetchModelsFromDataLearner()`：fetch 18 页 HTML，正则提取模型名，按关键词映射
  - `saveFetchedModels()`：保存到 chrome.storage 并更新内存覆盖
  - `resetToDefaultModels()`：清除所有自定义模型，恢复默认
  - 文件：`src/services/modelUpdater.js`
- **[自定义模型存储]** 更新 `src/services/storage.js`，新增 `getCustomModels` / `saveCustomModels` / `clearAllCustomModels`
- **[内存优先模型加载]** `platforms.js` 中 `getModelsByPlatform` 改为同步函数，优先读取内存中的 `_customModels`，未覆盖则回退到硬编码列表
  - 新增 `setCustomModels` / `clearCustomModels` / `getCustomModelsState` / `loadCustomModelsFromStorage`
  - 文件：`src/data/platforms.js`
- **[设置页增加更新按钮]** `src/OptionsApp.vue` 右侧设置面板增加「更新模型列表」和「恢复默认」按钮
  - 更新时显示 loading 状态，成功/失败 toast 提示
  - 文件：`src/OptionsApp.vue`
- **[popup 增加刷新按钮]** `src/App.vue` header 区域增加刷新图标按钮，一键同步 DataLearner 最新模型，不用打开设置页
  - 文件：`src/App.vue`
- **[manifest 权限] 增加 `host_permissions: ["https://www.datalearner.com/*"]`，允许扩展请求 DataLearner
  - 文件：`public/manifest.json`
- **[启动加载自定义模型]** `main.js` 和 `options.js` 启动时调用 `loadCustomModelsFromStorage()`，确保内存中已有用户自定义模型
  - 文件：`src/main.js`、`src/options.js`

## v2.1.1 (2026-05-13) 表单增加「API 模型」字段，与「官方模型」区分

### 新增特性
- **[双模型字段]** 表单中「模型」改名为「官方模型」（从平台下拉列表选择，用于展示），新增「API 模型」自由文本输入框（用于第三方 API 中转时的实际调用名）
  - 留空时向后兼容，复制/导出仍用官方模型名
  - 填写后 TokenCard 显示为 `官方模型 (API: api模型名)`，蓝色标注
  - 文件：`src/components/TokenForm.vue`、`src/components/TokenCard.vue`
- **[ccswitch 配置使用 API 模型名]** `copyTokenConfig` 中 5 个 `_MODEL` 字段优先使用 `token.apiModel`，未填写时回退到 `token.model`
  - 文件：`src/services/clipboard.js`

## v2.0.8 (2026-05-12) 插件改名：AI Token Manager → Token 管理器

### 变更
- **[插件改名]** 用户要求改为中文且不带「AI」，统一更新为「Token 管理器」
  - 运行时可见：`public/manifest.json` name、`index.html`/`options.html` title、`src/App.vue` popup H1、`src/OptionsApp.vue` 设置页 H1 + 脚注版本号
  - 项目元数据：`package.json` / `package-lock.json` 顶层 name 改为 `token-manager`（npm 包名不接受中文）
  - 注释与 log：`generate-icons.js` 头部注释与 console.log
  - 文档：`需求方案.md` 顶部标题与「项目名称」字段
  - 不动：`实现计划*.md`（历史文档）、`更改记录.md` 历史条目、`dist/`（下次 build 自动更新）、`package.json`/`manifest.json` 的 description（描述用途，不是名字）
  - 文件：`public/manifest.json`、`index.html`、`options.html`、`src/App.vue`、`src/OptionsApp.vue`、`package.json`、`package-lock.json`、`generate-icons.js`、`需求方案.md`

## v2.0.7 (2026-05-12) 修复：「复制完整配置」格式改为 ccswitch profile JSON 片段

### Bug 修复
- **[复制配置格式错误]** 「复制完整配置」原本输出 `平台名 | 模型 | Key | API地址` 管道符拼接，无法直接粘贴进 ccswitch 配置文件；ccswitch 实际要的是 `env` 环境变量 JSON 块
  - 根因：早期需求方案 2.2 节对 ccswitch 配置格式认知错误，把管道符拼接当成了 ccswitch 格式
  - 解决方案：`copyTokenConfig` 改为输出 `"<token.name>": { env: {...7 字段...}, includeCoAuthoredBy: false }` 形式的 JSON 片段，可直接粘贴进 ccswitch 配置文件的 profiles 父对象；5 个 _MODEL 字段统一填 `token.model`；profile key 使用 `token.name`；用 `JSON.stringify` 处理 value 自动 escape 特殊字符
  - 文件：`src/services/clipboard.js`、`需求方案.md`（2.2 节同步）

## v2.0.6 (2026-05-12) TokenCard 突出 API 地址与 API Key，两者独立可复制

### 新增特性
- **[TokenCard 三向复制]** API 地址与 API Key 视觉对等，各自独立行框 + 独立复制图标按钮；同时保留 ccswitch 全量配置拼接复制能力
  - 头部 `.card-actions` 新增「复制全量配置」图标按钮（与编辑/删除并列），复制 `平台名 | 模型 | Key | API地址`
  - API 地址改为浅色行框 `.token-url-row`（与 Key 行框样式一致），明文显示，过长 ellipsis 截断，悬停 title 展示完整 URL，右侧 icon 复制 `baseUrl`
  - API Key 行框去掉文字「复制」按钮，改为图标按钮，仅复制 `apiKey` 单字段
  - Toast 文案区分：「已复制配置」「已复制 API 地址」「已复制 Key」，多次连续点击复用同一 toast（清除上次 setTimeout 避免重叠）
  - 文件：`src/components/TokenCard.vue`
- **[需求方案 2.2]** 一键复制章节同步更新，明确三种独立复制行为及 API 地址明文显示规则
  - 文件：`需求方案.md`

## v2.0.5 (2026-05-12) 修复：多卡片时卡片被压缩

### Bug 修复
- **[卡片高度被压缩]** 添加多个 Token 后，每张 TokenCard 自动变小以塞进 popup，期望应为卡片大小不变、列表滚动
  - 根因：①`src/styles/common.css` body 仅设 `min-height` 未设 `height`，popup 高度随内容撑高直到浏览器上限；②`src/App.vue` 中 `.token-list` 是 flex 列容器，而 `.token-card` 默认 `flex-shrink: 1`，flex 容器在内容溢出时按比例压缩子项而非触发 overflow scroll
  - 解决方案：①body 由 `min-height: 480px` 改为固定 `height: 600px`，popup 尺寸恒定 400×600；②`.token-card` 增加 `flex-shrink: 0`，卡片不再被 flex 容器压缩，超出时由 `.token-list` 的 `overflow-y: auto` 触发垂直滚动
  - 文件：`src/styles/common.css`、`src/components/TokenCard.vue`

## v2.0.4 (2026-05-12) App.vue 主界面

### 新增
- **[App.vue]** 主界面组件实现
  - 顶部 Header：标题「Token Manager」+ Token 数量统计、搜索输入框、添加按钮
  - Token 列表：使用 TokenCard 组件渲染，支持搜索过滤（按名称/平台/模型/API地址搜索）
  - 空状态：未添加 Token 时显示引导页面；搜索无结果时显示提示
  - 底部工具栏：导入/导出 JSON 文件功能
  - 集成 TokenForm（抽屉弹窗）和 ConfirmDialog（删除确认）
  - 文件：`src/App.vue`
- **[main.js]** 应用入口文件，配置 Vue + ElementPlus + 中文语言包
  - 文件：`src/main.js`

## v2.0.3 (2026-05-12) TokenForm 组件重新设计

### 重构
- **[TokenForm]** TokenForm 组件重新设计，从弹窗改为右侧抽屉（el-drawer）
  - 平台选择：分组下拉框，显示彩色圆点标识各平台
  - 模型选择：可筛选、可自定义输入的模型选择框，根据平台动态加载
  - 切换平台时自动填充 API 地址
  - 自动保存：添加模式下监听表单变化自动保存草稿到 localStorage
  - 草稿恢复：打开抽屉时自动恢复上次未保存内容，显示恢复提示横幅
  - 表单字段：平台 / 模型 / 名称 / API 地址 / API Key / 备注
  - 文件：`src/components/TokenForm.vue`

## v2.0.2 (2026-05-12) TokenCard 组件重新设计

### 重构
- **[TokenCard]** TokenCard 组件重新设计为简洁现代风格
  - 左侧彩色平台徽标（platform badge）+ 分隔线
  - 右侧卡片内容：名称、模型、API 地址、API Key（脱敏/显示切换）、复制按钮、编辑/删除按钮
  - API Key 默认脱敏显示，点击眼睛图标切换显示/隐藏
  - 一键复制：复制「平台名 | 模型 | Key | API 地址」格式配置
  - 复制成功 Toast 提示（带动画）
  - 悬停阴影效果，响应式布局
  - 文件：`src/components/TokenCard.vue`

## v2.0.0 (2026-05-12) 项目重构初始化

### 重构
- **[清理旧文件]** 删除 src/ 下所有旧文件（保留 src/ 目录本身）、删除空子目录
- **[package.json]** 更新：name=ai-token-manager, version=2.0.0, description, 移除 sharp 依赖，调整依赖结构
  - 文件：`package.json`
- **[vite.config.js]** 更新：修改 viteStaticCopy 只复制 manifest.json（移除 icons 复制），保留双入口配置
  - 文件：`vite.config.js`
- **[manifest.json]** 更新：version=2.0.0, description, permissions 简化为 ["storage"]，移除 notifications
  - 文件：`public/manifest.json`
- **[index.html]** 新建入口页面，指向 /src/main.js
  - 文件：`index.html`
- **[options.html]** 新建设置页面，指向 /src/options.js
  - 文件：`options.html`

## v0.0.6 (2026-05-12) Options 设置页

### 新增特性
- **[OptionsApp]** 创建设置页面，支持配置默认提醒天数、排序方式及数据导出/导入/清空
  - 文件：`src/OptionsApp.vue`
- **[options.js]** 创建 Options 页面 Vue 3 + Element Plus 入口
  - 文件：`src/options.js`

## v0.0.5 (2026-05-12) ConfirmDialog 确认删除弹窗组件

### 新增特性
- **[ConfirmDialog]** 创建确认删除弹窗组件
  - v-model 控制显示/隐藏，props: modelValue, tokenName
  - emits: update:modelValue, confirm
  - 文件：`src/components/ConfirmDialog.vue`

## v0.0.3 (2026-05-12) TokenCard 卡片组件

### 新增特性
- **[TokenCard]** 创建卡片组件，展示单个 Token 的平台、名称、API Key、额度及备注
  - 文件：`src/components/TokenCard.vue`

## v0.0.4 (2026-05-12) TokenForm 表单组件

### 新增特性
- **[TokenForm]** 创建弹窗表单组件，支持添加和编辑 Token
  - 依赖 `src/utils/platforms.js` 中的 `PLATFORM_LIST`
  - 通过 `token` prop 判断添加/编辑模式，emits `update:modelValue` 和 `submit`
  - 文件：`src/components/TokenForm.vue`

## v2.0.1 (2026-05-12) 平台配置数据

### 新增特性
- **[platforms.js]** 创建 18 个 AI 平台配置数据文件
  - 包含：国际大厂 (7)、国内大厂 (11)、聚合平台 (3)、其他 (2)
  - 每个平台：id / name / nameCn / color / logo / baseUrl / models
  - 导出工具函数：getPlatform / getModelsByPlatform / maskApiKey / PLATFORM_GROUPS / PLATFORM_LIST
  - 文件：`src/data/platforms.js`