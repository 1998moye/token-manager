# 更改记录

## v2.2.0 (2026-05-14) 通用 Token 支持

### 新增特性
- **[通用 Token 管理]** 新增「通用 Token」标签页，支持保存只需要一个 token 字符串的普通凭证（GitHub Token、npm Token 等）
  - 字段：名称 / Token 值 / 分类（git/npm/cloud/other/自定义）/ 备注
  - 分类彩色标签展示，Token 值脱敏显示，支持显示/隐藏切换
  - 文件：`src/components/GeneralTokenCard.vue`、`src/components/GeneralTokenForm.vue`、`src/data/generalCategories.js`
- **[标签页切换]** Popup 和设置页均支持「AI Token」「通用 Token」标签页切换
  - 切换时搜索清空，列表/添加/导入导出行为自动隔离
  - 文件：`src/App.vue`、`src/OptionsApp.vue`
- **[通用 Token Storage API]** 新增 getGeneralTokens/addGeneralToken/updateGeneralToken/deleteGeneralToken/saveGeneralTokens
  - 存储 key 为 `generalTokens`，与 AI Token（`tokens`）完全独立
  - 文件：`src/services/storage.js`
- **[导入导出隔离]** AI Token 和通用 Token 各自独立导入导出，数据结构保持纯数组不变
- **[表单自动保存]** 通用 Token 表单支持自动保存草稿到 localStorage（key 为 `generalTokenDraft`）
  - 文件：`src/composables/useAutoSave.js`（支持自定义 storage key）
